{
  "name": "video-quiz-app",
  "version": "1.0.0",
  "description": "A video quiz application with AI-powered chat",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:frontend",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npm run dev:frontend",
    "build": "cd frontend && npm run build",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}